# public/.htaccess

Options -Indexes
RewriteEngine On

# Jika file atau folder fisik tidak ada, arahkan semua ke index.php
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]